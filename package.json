{
  "name": "multi-proposal-voter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "check": "node --check app.js && node --test",
    "dev": "vercel dev",
    "test": "node --test"
  }
}
